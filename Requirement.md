# Requirement — QL-TAISAN

Dự án này được **onboard từ source code có sẵn** (2026-08-25, repo GitHub `anhnt205/PM_Quan_Ly_Tai_San`), không tạo mới từ template — nên không có tờ trình/spec yêu cầu gốc để chép vào đây.

Yêu cầu/nghiệp vụ thật của hệ thống được **suy ra từ chính code đang chạy** (89 controller, 136 migration, giao diện React) và ghi lại đầy đủ tại [`docs/REQUIREMENTS_AS_IS.md`](docs/REQUIREMENTS_AS_IS.md) — đó là tài liệu tham chiếu chính cho dự án này, không phải file này.

File này chỉ tồn tại để thoả quy ước bắt buộc của factory (mỗi project phải có `Requirement.md`, xem `CLAUDE.md` ở gốc repo).
